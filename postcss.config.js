/** postcss.config.js — Tailwind CSS v4 (CSS-first) + @tailwindcss/postcss
 * v4 รวม autoprefixer ภายในแล้ว ไม่ต้องประกาศแยก
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;